# 05 構築手順書 - 取引の手動入力・一覧・編集画面実装

## 目次

- [1. 概要](#1-概要)
- [2. 前提条件](#2-前提条件)
- [3. 手順](#3-手順)
  - [3.1. フォームを実装する](#31-フォームを実装する)
  - [3.2. ビューを実装する](#32-ビューを実装する)
  - [3.3. 共通レイアウト（base.html）を作成する](#33-共通レイアウトbasehtmlを作成する)
  - [3.4. URLを設定する](#34-urlを設定する)
  - [3.5. テンプレートを作成する](#35-テンプレートを作成する)
  - [3.6. Meta.widgetsのlint除外を追加する](#36-metawidgetsのlint除外を追加する)
  - [3.7. テストを追加する](#37-テストを追加する)
  - [3.8. 動作確認](#38-動作確認)

## 1. 概要

対応Issue: [#22](https://github.com/Seiya-Nakagawa/kakeibo/issues/22)

取引の一覧（絞り込み・CSV出力）、登録・編集（支出/収入切替）画面を実装する。
対応する設計書: [基本設計書5.3.2節](../02.設計/基本設計書.md#532-取引一覧3)、
[5.3.3節](../02.設計/基本設計書.md#533-取引登録編集4)

## 2. 前提条件

- [04_マスタ管理画面実装.md](04_マスタ管理画面実装.md) の完了により、Category等のマスタが
  Adminから登録できる状態であること

## 3. 手順

### 3.1. フォームを実装する

`webapp/kakeibo/forms.py` を新規作成する。

```python
from django import forms

from kakeibo.models import Category, Transaction


class TransactionForm(forms.ModelForm):
    """画面4 取引登録・編集: 種別（支出/収入）でフォーム項目の要否が変わる（基本設計書5.3.3節）。"""

    class Meta:
        model = Transaction
        fields = (
            "transaction_type",
            "transaction_date",
            "amount",
            "counterpart",
            "category",
            "payment_method",
            "account",
            "memo",
            "is_excluded_from_aggregation",
        )
        widgets = {
            "transaction_type": forms.RadioSelect,
            "transaction_date": forms.DateInput(attrs={"type": "date"}),
        }

    def clean(self):
        cleaned_data = super().clean()
        transaction_type = cleaned_data.get("transaction_type")
        category = cleaned_data.get("category")

        if transaction_type == Transaction.TransactionType.EXPENSE:
            if not cleaned_data.get("payment_method"):
                self.add_error("payment_method", "支出には決済手段が必須です。")
            cleaned_data["account"] = None
        elif transaction_type == Transaction.TransactionType.INCOME:
            if not cleaned_data.get("account"):
                self.add_error("account", "収入には入金先口座が必須です。")
            if not category:
                self.add_error("category", "収入にはカテゴリが必須です。")
            cleaned_data["payment_method"] = None

        if category and transaction_type and category.category_type != transaction_type:
            self.add_error("category", "選択したカテゴリは種別（支出/収入）と一致しません。")

        return cleaned_data


class TransactionFilterForm(forms.Form):
    """画面3 取引一覧の絞り込み条件（基本設計書5.3.2節）。"""

    date_from = forms.DateField(required=False, widget=forms.DateInput(attrs={"type": "date"}))
    date_to = forms.DateField(required=False, widget=forms.DateInput(attrs={"type": "date"}))
    category = forms.ModelChoiceField(queryset=Category.objects.all(), required=False)
    payment_method = forms.ModelChoiceField(queryset=None, required=False)
    amount_min = forms.IntegerField(required=False, min_value=0)
    amount_max = forms.IntegerField(required=False, min_value=0)
    counterpart = forms.CharField(required=False)

    def __init__(self, *args, **kwargs):
        super().__init__(*args, **kwargs)
        from kakeibo.models import PaymentMethod

        self.fields["payment_method"].queryset = PaymentMethod.objects.all()
```

- 支出/収入で必須項目が変わるバリデーションを`clean()`に実装する
- 「選択したカテゴリの`category_type`が取引種別と一致するか」もあわせて検証する

### 3.2. ビューを実装する

`webapp/kakeibo/views.py` に以下を追加する。

- `_filtered_transactions(request)`: 一覧・CSV出力で共通の絞り込みロジック
  （論理削除されていないものに限定し、`TransactionFilterForm`の各条件でフィルタする）
- `TransactionListView`（`ListView`、`paginate_by=50`）
- `TransactionExportCsvView`（`View`）: 絞り込み結果をExcelで文字化けしないよう
  BOM付きUTF-8のCSVとして出力する
- `TransactionCreateView`（`CreateView`）: `source=manual`、`created_by=request.user`を設定する
- `TransactionUpdateView`（`UpdateView`）: 論理削除されていない取引のみ対象とする
- `TransactionDeleteView`（`View`）: `is_deleted=True`を設定する論理削除（要件2.1で物理削除しない）

### 3.3. 共通レイアウト（base.html）を作成する

以降のIssueで画面が増えるため、`webapp/kakeibo/templates/kakeibo/base.html`を新規作成し、
ナビゲーション（ダッシュボード・取引一覧・取引登録・未分類取引・資産残高・マスタ管理・ログアウト）
とmessagesフレームワークの表示を共通化する。既存の`home.html`をこれを継承する形に修正する。

ナビゲーションが参照する`dashboard`・`unclassified-transaction-list`・`account-balance`の
URL名は、実体を後続Issue（#25、#23、#26）で実装するため、本Issue時点では`HomeView`への
暫定割当としてURL名のみ先行予約する。

### 3.4. URLを設定する

`webapp/config/urls.py` に取引関連のURLを追加する。

```python
path("transactions/", views.TransactionListView.as_view(), name="transaction-list"),
path(
    "transactions/export/",
    views.TransactionExportCsvView.as_view(),
    name="transaction-export-csv",
),
path("transactions/new/", views.TransactionCreateView.as_view(), name="transaction-create"),
path(
    "transactions/<int:pk>/edit/",
    views.TransactionUpdateView.as_view(),
    name="transaction-update",
),
path(
    "transactions/<int:pk>/delete/",
    views.TransactionDeleteView.as_view(),
    name="transaction-delete",
),
```

### 3.5. テンプレートを作成する

`webapp/kakeibo/templates/kakeibo/transaction_list.html`（絞り込みフォーム・一覧テーブル・
CSV出力リンク・ページネーション）と`webapp/kakeibo/templates/kakeibo/transaction_form.html`
（登録・編集共通フォーム。種別のラジオボタン切替に応じて決済手段/入金先口座の項目表示を
切り替える軽量JSを含む）を新規作成する。いずれも`base.html`を継承する。

### 3.6. Meta.widgetsのlint除外を追加する

`webapp/pyproject.toml`の`[tool.ruff.lint.per-file-ignores]`に、`models.py`と同様の理由
（フォーム定義時にのみ参照される規約上の書き方であり、RUF012の懸念が当てはまらない）で
`forms.py`を追加する。

```toml
"**/forms.py" = ["RUF012"]
```

### 3.7. テストを追加する

`webapp/kakeibo/tests/test_transactions.py`に以下を追加する。

- 支出登録時に決済手段が必須であること、収入登録時に入金先口座・カテゴリが必須であること
- カテゴリの`category_type`が取引種別と不一致の場合にエラーになること
- 登録時に`source=manual`・`created_by`が設定されること
- 一覧が論理削除済みを除外すること、金額範囲で絞り込めること
- CSV出力に絞り込み結果のみが含まれること
- 削除操作が論理削除であること

### 3.8. 動作確認

```bash
cd webapp
uv run python manage.py test kakeibo
uvx ruff check .
uvx ruff format --check .
```
